## Формулировка

Сложение вещественных чисел корректно: если $x = [(\alpha_n)] = [(\alpha'_n)]$ и $y = [(\beta_n)] = [(\beta'_n)]$, то

$$[(\alpha_n + \beta_n)] = [(\alpha'_n + \beta'_n)]$$

---

## Основано на

- [[O11. Сложение вещественных чисел]]
- [[D21. Эквивалентность последовательностей]]
- [[D20. Сходящаяся в себе последовательность]]
- [[O7. Сложение рациональных чисел]]
- [[D9. Абсолютная величина]]
- [[D6. Равенство]]

---

## Доказательство

Из $(\alpha_n) \sim (\alpha'_n)$ следует, что для любого $\varepsilon > 0$ существует $N_1$ такое, что для всех $n \ge N_1$:

$$|\alpha_n - \alpha'_n| < \frac{\varepsilon}{2}$$

Аналогично, из $(\beta_n) \sim (\beta'_n)$ следует существование $N_2$ такого, что для всех $n \ge N_2$:

$$|\beta_n - \beta'_n| < \frac{\varepsilon}{2}$$

Тогда для $n \ge \max(N_1, N_2)$:

$$|(\alpha_n + \beta_n) - (\alpha'_n + \beta'_n)| \le |\alpha_n - \alpha'_n| + |\beta_n - \beta'_n| < \frac{\varepsilon}{2} + \frac{\varepsilon}{2} = \varepsilon$$

Следовательно, последовательности $(\alpha_n + \beta_n)$ и $(\alpha'_n + \beta'_n)$ эквивалентны. Значит, их классы эквивалентности совпадают. Лемма доказана.